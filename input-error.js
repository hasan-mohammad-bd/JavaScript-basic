function add(num1, num2) {
    const sum = num1 + num2;
    return sum;
}

const firstTotal = add(63, 7);
const secondTotal = add(54, 981);
const 

function multiply(num1, num2) {
    const result = num1 * num2
    return result;
}

const Result = multiply(13, 12); //here must be "," otherwise, there will be "non" output.
console.log(Result);